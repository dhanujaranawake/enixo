//
// Created by Dhanuja Ranawake on 12/18/17.
//


/* ©2017 Dhanuja Ranawake. All rights reserved.
#include <stdio.h>

int main() {
    int first, second, third;
    printf("Enter the first number:");
    scanf(" %d", &first);
    printf("Enter the second number:");
    scanf(" %d", &second);
    printf("Enter the third number:");
    scanf(" %d", &third);

    if(first>second){
        if(second>third){
            printf("largest value is %d and the smallest value is %d", first, third);
        }else {
            printf("largest value is %d and the smallest value is %d", first, second);
        }
    }else if (second>third){
        if(third>first){
            printf("largest value is %d and the smallest value is %d", second,first );
        }else {
            printf("largest value is %d and the smallest value is %d", second, third);
        }
    }else {
        if(second>first){
            printf("largest value is %d and the smallest value is %d", third, first);
        }else {
            printf("largest value is %d and the smallest value is %d", third, second);
        }
    }

    return 0;
}*/
